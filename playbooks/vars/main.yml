# Global vars
---
scdrm_install_dir: INSTALLDIR
shebang: "#!/bin/bash"
hebang: "#!/bin/ksh"
git_branch: main
myuser: USER
# These directories are managed by Git
check_directory: "/etc /usr/lib/systemd /usr/lib64/security"
dirlist:
  - /etc
  - /usr/lib/systemd
  - /usr/lib64/security
...
